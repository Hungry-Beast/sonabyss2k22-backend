# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - Shristi-backend

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
   build-and-deploy:
    runs-on: ubuntu-latest
     # runs-on: ubuntu-latest
    # needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      - name: Setup Node.js version
        uses: actions/setup-node@v3
        with: 
          node-version: '20.x'
      - name: Install Dependencies
        run: npm install
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'shristi-backend'
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .

      

      # - name: Login to Azure
      #   uses: azure/login@v1
      #   with:
      #     client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_E2FA00D709424E2DA73368C0B86F4DF7 }}
      #     tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_47C0716ADA9F4123889F726E44D077A2 }}
      #     subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_0A7D77820D964BC9BAC87FDC9C8C529B }}
      # - name: 'Deploy to Azure Web App'
      #   uses: azure/webapps-deploy@v2
      #   id: deploy-to-webapp
      #   with:
      #     app-name: 'Shristi-backend'
      #     slot-name: 'Production'
      #     package: .


        
      # - name: Deploy to Azure App Service
      #   uses: azure/webapps-deploy@v2
      #   with:
      #     app-name: integrationninjas
      #     publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
      #     package: .       





  # build:
  #   runs-on: windows-latest

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Set up Node.js version
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '20.x'

  #     - name: npm install, build, and test
  #       run: |
  #         npm i
 
      

  # deploy:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   environment:
  #     name: 'Production'
  #     url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
  #   permissions:
  #     id-token: write #This is required for requesting the JWT

  #   steps:
  #     # - name: Download artifact from build job
  #     #   uses: actions/download-artifact@v3
  #     #   with:
  #     #     name: node-app
      
  #     - name: Login to Azure
  #       uses: azure/login@v1
  #       with:
  #         client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_E2FA00D709424E2DA73368C0B86F4DF7 }}
  #         tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_47C0716ADA9F4123889F726E44D077A2 }}
  #         subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_0A7D77820D964BC9BAC87FDC9C8C529B }}

  #     - name: 'Deploy to Azure Web App'
  #       uses: azure/webapps-deploy@v2
  #       id: deploy-to-webapp
  #       with:
  #         app-name: 'Shristi-backend'
  #         slot-name: 'Production'
  #         package: .
          
